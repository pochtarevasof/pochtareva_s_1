//-- HTTPConnection.bsl
&НаКлиенте
Процедура ПодключениеИз1С(Команда)
    Попытка
        // Внешнее HTTP-подключение
        СоединениеHTTP = Новый HTTPСоединение("www.mysite.ru");
    Исключение
    КонецПопытки;
КонецПроцедуры
//== HTTPConnection.bsl
