//-- RequestUserInfo.bsl
Функция РабочееМестоКлиента() Export
    // ИмяПользователя
    имя = ИмяПользователя();
    // ПолноеИмяПользователя
    полнИмя = ПолноеИмяПользователя ();
    // ИмяКомпьютера
    комп = ИмяКомпьютера();
    Возврат имя + " (" + полнИмя + ") <-> " + комп ; 
КонецФункции
//== RequestUserInfo.bsl
