//-- RequestUserInfo.bsl
Функция РабочееМестоКлиента() Export
    // ИмяПользователя
    имя = ИмяПользователя();
    // ПолноеИмяПользователя
    полнИмя = ПолноеИмяПользователя ();
    
    Возврат имя + " (" + полнИмя + ")"; 
КонецФункции
//== RequestUserInfo.bsl
