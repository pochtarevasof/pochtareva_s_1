//-- WeakHash.bsl
Функция ПроверитьТекущийПарольПользователя(Пароль, РегистроНеза=Истина) Экспорт

    // Слабый алгоритм хеширования
    ХешMD2 = Новый ХешированиеДанных(ХешФункция.MD2);
    // Слабый алгоритм хеширования
    ХешMD5 = Новый ХешированиеДанных(ХешФункция.MD5);
    // Слабый алгоритм хеширования
    ХешSHA1 = Новый ХешированиеДанных(ХешФункция.SHA1);

КонецФункции
//== WeakHash.bsl