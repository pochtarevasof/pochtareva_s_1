//-- NavigationLinkClick.bsl
&НаКлиенте
Процедура СсылкаНаДокументОткрытие(Элемент, СтандартнаяОбработка)
    НавСсылка = Элемент.ТекстРедактирования;
    СсылкаНаДокумент = УзнатьНавигационнуюСсылка(НавСсылка);

    Попытка
        // Переход по навигационной ссылке
        ПерейтиПоНавигационнойСсылке(СсылкаНаДокумент);
    Исключение
        СообщениеПользователю.Текст = "Ошибка!";
        Сообщениепользователю.Сообщить();
    КонецПопытки;
КонецПроцедуры
//== NavigationLinkClick.bsl